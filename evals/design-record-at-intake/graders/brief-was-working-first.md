---
type: regex
target: { source: file, path: design.json }
pattern: "\"id\":\\s*\"brief\"[\\s\\S]{0,400}?\"startedAt\":"
match: contains
---

`init` left `brief` working while the questions were composed (the script stamps `startedAt`); the gate came after — the record never said "needs you" with nothing to answer.
