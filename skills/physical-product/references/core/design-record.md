# Design record — the project's memory of what was decided

One file in the project root answers "where is this design, what did the user
decide, what did I decide for them, and what is out of date" — for this session,
for the next one, and for Robion's design panel:

```
<project>/design.json
```

The transcript is not that memory: it is lost on a fresh session, it fills up
with render output, and nobody can edit an answer in it. The record is a plain
JSON file, committed with the sources, regenerated by nothing — you write it.

## Rules

- **Create it at the end of the brief analysis** (SKILL.md §1 step 1), before
  the question round: idea, spec, routed verticals, the stage list with every
  stage `pending`. The project directory exists by then (the task's working
  directory); scaffolding (§4) comes later and never touches this file.
- **Update it at every gate** — an answered question round, the concept
  freeze, each stage of §5 — and whenever a change request arrives (§7). A
  gate is not passed until the record says so.
- **Every answer records who decided.** `decidedBy: user` when the user
  answered, `default` when they accepted the recommended default, `claude`
  when you chose (a fact the user cannot know, or an explicit "decide for
  me") — then `reason` is mandatory and the same sentence goes into the build
  sheet's assumptions section.
- **Stage ids are fixed** (below); the app's map relies on them. Labels are
  yours, in the user's language. A stage the stack has no equivalent for
  (analysis on a sewn bag) is `skipped` with a note, never removed.
- **A change after a gate is logged, not silently absorbed.** Append a
  `changes` entry naming what changed and which done stages it invalidates,
  set those stages to `stale`, tell the user, then regenerate in order.
  Stale means "was done, inputs changed since"; it becomes `done` again only
  after the stage is regenerated.
- **Structure in English, content in the user's language.** Keys, ids, enums
  and timestamps are the standard; `label`, `summary`, `answer`, `reason`,
  `note`, `title` are written the way the user speaks.
- **Paths are project-relative** and point at real files (`out/viz_hero.png`,
  `model.py`); the app shows them as thumbnails and links.
- **Never store secrets, absolute paths, or the transcript.** The record is
  small — one screen of JSON, not a log.

## Schema by example

```json
{
  "version": 1,
  "updated": "2026-09-08T14:02:00Z",
  "product": { "name": "Workbench 1800", "slug": "workbench-1800", "language": "en" },

  "idea": {
    "text": "a sturdy workbench for hand planing, about 1.8 m, beech if affordable",
    "attachments": [".robion/uploads/bench-photo.jpg"]
  },

  "spec": {
    "summary": "Free-standing workbench, 1800 × 800 × 900 mm, solid beech top 40 mm; must resist racking under hand planing; built with a track saw and router, no CNC.",
    "verticals": ["woodworking"],
    "stack": "solids",
    "open": ["drawer yes/no", "finish", "casters"]
  },

  "questions": [
    {
      "id": "load",
      "group": "function",
      "label": "Load case",
      "answer": "150 kg static, planing impacts",
      "default": "100 kg static",
      "decidedBy": "user",
      "answeredAt": "2026-09-08T13:41:00Z"
    },
    {
      "id": "width",
      "group": "dimensions",
      "label": "Width",
      "answer": "1800",
      "default": "1600",
      "decidedBy": "user",
      "answeredAt": "2026-09-08T13:58:00Z"
    },
    {
      "id": "finish",
      "group": "materials",
      "label": "Finish",
      "answer": "hard wax oil",
      "default": "hard wax oil",
      "decidedBy": "claude",
      "reason": "user asked me to decide; oil is repairable on a work surface and is in the workshop profile",
      "answeredAt": "2026-09-08T13:41:00Z"
    }
  ],

  "concept": {
    "variants": [
      { "id": "a", "title": "Four legs, apron", "summary": "Simplest build, softer against racking.", "image": "out/concept_a.png", "facts": { "mass_kg": 62, "build_h": 6 } },
      { "id": "b", "title": "Frame with stretchers", "summary": "Stretchers low plus a centre rail — stiffest for planing.", "image": "out/concept_b.png", "facts": { "mass_kg": 71, "build_h": 8 } }
    ],
    "recommended": "b",
    "chosen": "b",
    "frozenAt": "2026-09-08T14:02:00Z"
  },

  "stages": [
    { "id": "idea",       "label": "Idea",             "status": "done" },
    { "id": "brief",      "label": "Brief",            "status": "done" },
    { "id": "concept",    "label": "Concept",          "status": "done" },
    { "id": "model",      "label": "Model",            "status": "stale", "artifacts": ["model.py"], "note": "width changed 1600 → 1800" },
    { "id": "preview",    "label": "Previews",         "status": "stale", "artifacts": ["out/viz_hero.png"] },
    { "id": "drawings",   "label": "Drawings",         "status": "pending" },
    { "id": "bom",        "label": "Materials & cuts", "status": "pending" },
    { "id": "plan",       "label": "Build plan",       "status": "pending" },
    { "id": "analysis",   "label": "Strength",         "status": "pending" },
    { "id": "buildsheet", "label": "Build sheet",      "status": "pending" }
  ],

  "changes": [
    {
      "at": "2026-09-08T13:58:00Z",
      "what": "questions.width",
      "from": "1600",
      "to": "1800",
      "invalidates": ["model", "preview"],
      "note": "concept recomputed; a centre rail was added to variant b"
    }
  ]
}
```

Field notes:

- `stages[].status` ∈ `pending | working | needs_you | done | stale | skipped`.
  `working` = you are producing it now; `needs_you` = the gate waits for the
  user; `done` = the gate passed. Exactly one stage is `working` or `needs_you`
  at a time — that is the "current" stage.
- `stages[].artifacts` — the files the stage produced (project-relative).
- `questions[].id` — lowercase snake_case, stable across the project; when the
  Robion `brief` panel exists, use the same string as the control `key`, so a
  changed control and a record entry name the same thing.
- `concept.variants[].facts` — a flat object of numbers the user compares
  (mass, hours, stiffness ratio, cost only when the profile gives prices).
- `changes[].what` — a dotted path into the record (`questions.width`,
  `concept.chosen`, `spec.summary`).

## The ten stages

| id | SKILL.md | Gate |
|---|---|---|
| `idea` | §1, the user's words | exists as soon as the record does |
| `brief` | §1 step 1–2 (brief analysis + question round) | answers recorded |
| `concept` | §1 step 3 (concept gate) | `concept.chosen` + `frozenAt` |
| `model` | §5 stage 1 | user approves the live geometry |
| `preview` | §5 stage 2 | user approves proportions |
| `drawings` | §5 stage 3 (drawings or pattern sheets) | every PNG read and checked |
| `bom` | §5 stage 4 (BOM + cut plan / marker) | masses cross-checked |
| `plan` | §5 stage 5 (assembly / sewing / welding plan) | executable order |
| `analysis` | §5 stage 6 (analytic + FEA; `skipped` when not load-bearing) | sanity checks pass |
| `buildsheet` | §5 stage 7 | user approves |

## The map in Robion

When `set_controls` exists, the record has a visible twin: a `steps` control on
the task-scoped `brief` panel — the same panel that carries the question round.
Build it right after the record exists, before the first question:

```json
{
  "scope": "task", "panel": "brief", "panelLabel": "Design", "panelIcon": "route",
  "mode": "merge",
  "controls": [
    {
      "key": "design_map", "type": "steps", "label": "Design", "width": "full",
      "options": [
        { "value": "idea", "label": "Idea" }, { "value": "brief", "label": "Brief" },
        { "value": "concept", "label": "Concept" }, { "value": "model", "label": "Model" },
        { "value": "preview", "label": "Previews" }, { "value": "drawings", "label": "Drawings" },
        { "value": "bom", "label": "Materials & cuts" }, { "value": "plan", "label": "Build plan" },
        { "value": "analysis", "label": "Strength" }, { "value": "buildsheet", "label": "Build sheet" }
      ],
      "current": "brief"
    }
  ]
}
```

- Option values are the stage ids, labels are localized like the record's.
- At every gate: update the record, then `set_controls` with `mode: 'merge'`
  and only the `design_map` control carrying the new `current` (`'done'` after
  the build sheet is approved). The `steps` widget knows one running step;
  `stale` and `needs_you` exist only in the record until the app's design
  panel ships — say them in the chat.
- **Keep the `brief` panel after intake.** Its selects and texts stay editable;
  a value the user changes later arrives with their next prompt as a controls
  diff. Treat that as a change request (§7): update the answer in the record,
  log the change, mark the stale stages. Never `remove_controls` the panel.
- The question round on that panel: `select` / `toggle` / `text` per question,
  defaults preselected, and **the first option of every select is
  `decide_for_me`** (labelled in the user's language). One `send` button
  returns the answers as the next prompt; questions a `select` cannot hold
  go in the chat with the same ids.
- Bare terminal (no `set_controls`): the record alone; ask in the chat.

## Change protocol (§7 in record terms)

1. Identify what changed: a question, the chosen concept, a spec sentence, a
   parameter after the freeze. Update that field; set `decidedBy` /
   `answeredAt` again.
2. Append a `changes` entry; list in `invalidates` every `done` stage whose
   inputs it touches — by default every done stage after the changed one
   (linear); keep a later stage `done` only when you can say why it is
   unaffected (a finish change does not invalidate the cut plan).
3. Set those stages `stale`, tell the user what will regenerate and roughly
   how long (the project CLAUDE.md "Cost of a change" section), and continue
   from the first stale stage. Expensive stages wait for the user's go.
